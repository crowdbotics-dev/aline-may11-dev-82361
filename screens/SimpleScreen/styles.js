import React from 'react';
import { StyleSheet } from 'react-native';

export const custom = StyleSheet.create({
  deep: {
    color: "#4c4f69", fontSize: 12, fontWeight: "bold"
  },
  font: {
    color: '#7287fd',
  },
  background: {
    backgroundColor: "#ccd0da"
  }
});

export const deepStyle = custom.deep;
